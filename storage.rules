rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public reads (previews) but require auth for writes.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
